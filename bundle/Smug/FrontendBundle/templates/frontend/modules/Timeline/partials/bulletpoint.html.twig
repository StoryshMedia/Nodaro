<div class="flex flex-col sm:relative timeline--item">
    <h4 class="{{ headline.classes}}" {% if isBackendView(_context) %} {{ adminConfig(_context, 'headline', id, loop.index) }} {% endif %}>
        {{ headline.value }}
    </h4>
    <p class="{{ caption.classes }}" {% if isBackendView(_context) %} {{ adminConfig(_context, 'caption', id, loop.index) }} {% endif %}">
        {{ caption.value | raw }}
    </p>
</div>